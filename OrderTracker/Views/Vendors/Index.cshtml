@{
  Layout = "_Layout";
}

@using OrderTracker.Models;

<h1>Vendor:</h1>

@if (@Model.Count == 0)
{
  <h3>There are currently no vendors.</h3>
}

<h3><a href='/vendors/new'>Add a Vendor:</a></h3>

@foreach (Vendor vendor in Model)
{
  <hr><h3><a href='/vendors/@vendor.Id'>@vendor.Name</a></h3>
}